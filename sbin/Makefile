
-include $(TOPDIR)/Make.defs
-include $(SDKDIR)/Make.defs

CONFIG_TEST_SBIN_PRIORITY ?= SCHED_PRIORITY_DEFAULT
CONFIG_TEST_SBIN_STACKSIZE ?= 2048

APPNAME = sbin
PRIORITY = $(CONFIG_TEST_SBIN_PRIORITY)
STACKSIZE = $(CONFIG_TEST_SBIN_STACKSIZE)

ASRCS =
CSRCS =
MAINSRC = sbin_main.c

CONFIG_TEST_SBIN_PROGNAME ?= sbin$(EXEEXT)
PROGNAME = $(CONFIG_TEST_SBIN_PROGNAME)

include $(APPDIR)/Application.mk
